from rest_framework import serializers 
from .models import coupon 

class 
CouponSerialiozers(serializers.ModelSerializers):
class Meta:
  model = Coupon
  fields = ['id','code','discount','is_active','valid_from','valid_to']