from django.shortcuts import render
from .models import RestaurantInfo

def home (request):
phone = None
info = RestaurantInfo.objects.first()
if info:
       phone = info.phone 
       return render(request,'index.html',{'restaurant_phone':phone})