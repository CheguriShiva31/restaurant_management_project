from django.conf import settings
from datetime import datetime
def restaurant_info(request):
return{"restaurant_name": getattr(settings,"RESTAURANT_NAME",'="Our Restaurant"),"restaurant_hours": getattr(settings,"RESTAURANT_HOURS","hours not avaliable"),"year":datetime.now().year,}